const HOME = '/';

const AUTH = {
  LOGIN: '/login',
  SIGNIN: '/signin',
};

const CATEGORIES = '/categories';
const QUIZ = '/quiz';
const RANK = '/rank';

const ROUTES = {
  HOME,
  AUTH,
  CATEGORIES,
  QUIZ,
  RANK,
};

export default ROUTES;
