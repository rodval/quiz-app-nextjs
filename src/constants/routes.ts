const HOME = '/';

const AUTH = {
  SIGNIN: '/signin',
  SIGNUP: '/signup',
};

const CATEGORIES = '/categories';
const QUIZ = '/quiz';
const RANK = '/rank';

const ROUTES = {
  HOME,
  AUTH,
  CATEGORIES,
  QUIZ,
  RANK,
};

export default ROUTES;
