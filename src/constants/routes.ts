const HOME = '/';

const AUTH = {
  LOGIN: '/login',
  SIGNIN: '/signin',
};

const CATEGORIES = '/categories';
const QUIZ = '/quiz';

const ROUTES = {
  HOME,
  AUTH,
  CATEGORIES,
  QUIZ,
};

export default ROUTES;
